# This __init__.py file is for the 'agents.platform_mcp_client' package.
# It is intentionally left empty for now to avoid import errors
# related to legacy module structures.
# Core platform agent logic is in 'platform_agent.py' and node logic in 'platform_exec_node.py'.
