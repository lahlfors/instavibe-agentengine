# agents/app/common/__init__.py
# This file makes 'common' a package.
# You can optionally import modules here if you want to expose them directly,
# e.g., from .server import A2AServer
# For now, keeping it simple to ensure it's recognized as a package.
print("DEBUG: common package (re)loaded, server.py should be part of it") # Debug print
