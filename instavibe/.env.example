# This is an example environment configuration file for local development of the Instavibe app.
# Copy this file to .env in the same directory (instavibe/.env) and fill in your specific values.
# instavibe/.env should be added to .gitignore and NOT be committed to version control.

# --- Flask Specific ---
# Used for session management and other security features. Generate a strong random key.
# For development, any string will work, but keep it secret in production.
FLASK_SECRET_KEY="YOUR_DEVELOPMENT_SECRET_KEY_HERE"

# --- Google Cloud Specific ---
# Your Google Cloud Project ID where Spanner and other services are hosted.
GOOGLE_CLOUD_PROJECT="your-gcp-project-id"

# --- Spanner Configuration ---
# These are made mandatory by the application at startup.
SPANNER_INSTANCE_ID="instavibe-graph-instance"
SPANNER_DATABASE_ID="graphdb"

# --- Google Maps API Configuration (Optional) ---
# If not provided, mapping features might be disabled or limited.
GOOGLE_MAPS_API_KEY="YOUR_GOOGLE_MAPS_API_KEY_HERE_OR_LEAVE_BLANK"
GOOGLE_MAPS_MAP_ID="YOUR_GOOGLE_MAPS_MAP_ID_HERE_OR_LEAVE_BLANK"

# --- Flask Development Server Settings (Not used by Gunicorn in Cloud Run) ---
# Host for the local Flask development server (app.run())
APP_HOST="0.0.0.0"
# Port for the local Flask development server
APP_PORT="8080"
